// vercel.json
{
    "version": 2,
    "builds": [{ "src": "app.ts", "use": "@vercel/node" }],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/src/routers/api.ts"
      },
      { "src": "/(.*)", "dest": "/app.ts" }
    ]
  }
  